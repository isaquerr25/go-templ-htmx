package fertilization

import "github.com/isaquerr25/go-templ-htmx/views/templates"

templ FormFertilization(p FertilizationProps) {
	@templates.Base() {
		<section class="max-w-xl mx-auto mt-12 space-y-6">
			<header>
				<h2 class="text-3xl font-semibold"></h2>
			</header>
		</section>
	}
}
